{
  "version": 2,
  "builds": [
    {
      "src": "food_ordering_backend/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "food_ordering_backend/wsgi.py"
    }
  ]
}
